name: Tests

on:
  push:
    branches: main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Run tests
      run: echo "Running tests..."